/**
 * Shared layout dimensions for all-day event chips and placeholders.
 * These values are used across AllDayEventChip, AllDaySection,
 * AllDayPlaceholder, MonthSpanningChip, and CalendarGrid.
 */

/** Row height for all-day chips: chip height (20px) + gap (4px) */
export const ALL_DAY_ROW_HEIGHT = 24;

/** Standard border radius for event chips (px) */
export const CHIP_BORDER_RADIUS = "4px";

/** Left margin for all-day chips (px) */
export const CHIP_MARGIN_LEFT = 1;

/** Right margin/gap for all-day chips (px) */
export const CHIP_MARGIN_RIGHT = 4;
